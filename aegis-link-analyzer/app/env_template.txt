# ─────────────────────────────────────────────
# Aegis Link Analyzer — .env
# ─────────────────────────────────────────────

# ── Existing Keys (Phase 1) ──────────────────
VT_API_KEY=your_virustotal_key_here
URLSCAN_API_KEY=your_urlscan_key_here
PINECONE_API_KEY=your_pinecone_key_here
PINECONE_INDEX=your_index_name_here

# ── Ollama (local embedding model) ───────────
OLLAMA_HOST=http://host.docker.internal:11434
OLLAMA_MODEL=nomic-embed-text

# ── Phase 2: PhishTank API Key ────────────────
# Free signup at: https://www.phishtank.com/register.php
# Gives you 900 requests/hour (vs rate-limited without key)
PHISHTANK_API_KEY=your_phishtank_key_here

# ── Phase 2: Google Safe Browsing API Key ────
# Steps to get free key:
#   1. Go to: https://console.cloud.google.com
#   2. Create a new project (or use existing)
#   3. Search "Safe Browsing API" in the API Library
#   4. Click Enable
#   5. Go to Credentials → Create API Key
#   6. No billing required for Safe Browsing API
# Free tier: 10,000 requests/day
GSB_API_KEY=your_google_safe_browsing_key_here

# ── Phase 2: URLhaus ─────────────────────────
# NO KEY NEEDED — URLhaus is completely free and open
# https://urlhaus-api.abuse.ch/
